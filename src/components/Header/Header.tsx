import { Text, View } from 'react-native';

export default function Header() {
  return (
    <View>
      <Text style={{ fontSize: 20, fontWeight: 'bold', padding: 10, textAlign: 'center' }}>Header</Text>
    </View>
  );
}
